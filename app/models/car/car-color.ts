/**
 * Different colors a car can have.
 * With color the color of the marker is meant
 * which is used to show where the car is, when it's
 * parked.
 */
export enum CarColor {
  Red = 'Red',
  Green = 'Green',
  Blue = 'Blue',
  Black = 'Black',
  Yellow = 'Yellow',
  White = 'White',
  Purple = 'Purple',
  Brown = 'Brown',
  Orange = 'Orange',
}
